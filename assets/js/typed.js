// Typing bit
let title = new Typed('#website-name', {
    strings: ['<strong>Ingmar.dev</strong>'],
    typeSpeed: 40,
    cursorChar: '',
    startDelay: 250
});

let title2 = new Typed('#github', {
    strings: ['&nbsp;ingmarinho'],
    typeSpeed: 40,
    cursorChar: '',
    startDelay: 400
});

let title3 = new Typed('#instagram', {
    strings: ['&nbsp;ingm4rr'],
    typeSpeed: 40,
    cursorChar: '',
    startDelay: 800
});

let title4 = new Typed('#discord', {
    strings: ['&nbsp;Ingmar#0001'],
    typeSpeed: 40,
    cursorChar: '',
    startDelay: 1200
});

let title5 = new Typed('#steam', {
    strings: ['&nbsp;Ingmar'],
    typeSpeed: 40,
    cursorChar: '',
    startDelay: 1600
});

let title6 = new Typed('#onlyfans', {
    strings: ['&nbsp;Ingmar'],
    typeSpeed: 40,
    cursorChar: '',
    startDelay: 2000
});

