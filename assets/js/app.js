// Typing bit
let title = new Typed('#intro', {
    strings: ['<strong>Welcome to my website</strong>'],
    typeSpeed: 35,
    cursorChar: ''
});

let personal = new Typed('#personal', {
    strings: ['^2000 I\'m a Computer engineering student.'],
    typeSpeed: 35,
    cursorChar: ''
});

let webdev = new Typed('#webdev', {
    strings: ['^6000(Definitely not a web developer)'],
    typeSpeed: 35,
    cursorChar: ''
});

let socials = new Typed('#projects', {
    strings: ['^4000 <strong>Here are some of my projects:</strong>'],
    typeSpeed: 35,
    cursorChar: ''
});
